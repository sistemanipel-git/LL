let radius = 15;
radius = radius * radius;
console.log((Math.PI * radius).toFixed(2));

let length = 12;
let width = 22;
console.log((length * width).toFixed(2));

let height = 25;
console.log((Math.PI * radius * height).toFixed(2));
